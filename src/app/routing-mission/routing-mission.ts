import { Component } from '@angular/core';
import { RouterLink, RouterLinkActive } from '@angular/router';

@Component({
  selector: 'app-routing-mission',
  imports: [RouterLink, RouterLinkActive],
  templateUrl: './routing-mission.html',
  styleUrl: './routing-mission.css',
})
export class RoutingMission {}
